import{Injectable}from'@nestjs/common';import{InjectModel}from'@nestjs/mongoose';import{Model}from'mongoose';import{Comment,CommentDocument}from'./comment.schema';@Injectable()export class CommentsService{constructor(@InjectModel(Comment.name)private c:Model<CommentDocument>){}all(task:string){return this.c.find({task}).sort({createdAt:1})}create(task:string,author:string,name:string,d:any){return this.c.create({task,author,authorName:name,text:d.text})}}
