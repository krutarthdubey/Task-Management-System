'use client';import{useEffect,useState}from'react';import AppShell from'../../components/AppShell';import{api}from'../../lib/api';import{FolderKanban,Plus,Trash2}from'lucide-react';
export default function Projects(){const[p,setP]=useState<any[]>([]);const[n,setN]=useState('');const[d,setD]=useState('');const load=()=>api('/projects').then(setP);useEffect(()=>{load()},[]);const create=async()=>{if(!n.trim())return;await api('/projects',{method:'POST',body:JSON.stringify({name:n,description:d})});setN('');setD('');load()};const del=async(id:string)=>{if(confirm('Delete project?')){await api(`/projects/${id}`,{method:'DELETE'});load()}};return <AppShell title="Projects"><div className="grid lg:grid-cols-[1fr_340px] gap-6"><div className="grid md:grid-cols-2 gap-4">{p.map(x=><div className="panel p-5" key={x._id}><div className="flex items-start gap-3"><div className="w-10 h-10 rounded-xl flex items-center justify-center" style={{background:'color-mix(in srgb,var(--accent) 12%,transparent)',color:'var(--accent)'}}><FolderKanban size={20}/></div><div className="flex-1"><div className="font-bold">{x.name}</div><div className="muted text-sm mt-1">{x.description||'No description'}</div></div><button onClick={()=>del(x._id)}><Trash2 size={16}/></button></div></div>)}{!p.length&&<div className="panel p-10 text-center muted md:col-span-2">No projects yet.</div>}</div><div className="panel p-5 h-fit space-y-4"><h3 className="font-bold flex gap-2 items-center"><Plus size={18}/>Create project</h3><input className="input" placeholder="Project name" value={n} onChange={e=>setN(e.target.value)}/><textarea className="input min-h-24" placeholder="Description" value={d} onChange={e=>setD(e.target.value)}/><button className="btn btn-primary w-full" onClick={create}>Create project</button></div></div></AppShell>}
